# ORCA - Vercel Serverless Requirements
# Heavy ML packages (faiss-cpu, FlagEmbedding, xgboost) are excluded
# because they exceed Vercel's 250MB function size limit.
# The RAG pipeline gracefully falls back to TF-IDF when these are missing.

fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
sqlalchemy>=2.0.28
aiosqlite>=0.20.0
alembic>=1.13.1
httpx>=0.27.0
python-multipart>=0.0.9
numpy>=1.26.0
scikit-learn>=1.4.0
